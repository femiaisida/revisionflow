# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to GitHub.

# Firebase (get from Firebase Console → Project Settings)
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Mistral AI key is NOT set here.
# It must be added directly in Netlify Dashboard → Site settings → Environment variables.
# Variable name: MISTRAL_API_KEY (no VITE_ prefix — server-side only, never in the browser bundle)
# Get a free key at: https://console.mistral.ai
